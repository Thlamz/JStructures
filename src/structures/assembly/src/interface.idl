interface Heap {
    void Heap();
    void insert(long element, double value);
    long extract();
    long size();
};

interface Stack {
    void Stack();
    void push(long long element);
    long long pop();
    long size();
};